version: "3.7"

services:
    postgresql10:
        container_name: postgresql10
        image: manueltusset/postgresql:10.17
        ports:
            - 8080:8080
            - 5432:5432
        networks:
            - default
        volumes:
            - ./data:/var/lib/postgresql/data
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres
            - PGDATA=/var/lib/postgresql/data/pgdata
        deploy:
            resources: 
                limits: 
                    memory: '2G'
    pgadmin4:
        container_name: pgadmin4
        image: manueltusset/postgresql:pgadmin4
        ports:
            - 55432:80
        networks:
            - default
        environment:
            - PGADMIN_DEFAULT_PASSWORD=postgres
            - PGADMIN_DEFAULT_EMAIL=seuemail@gmail.com